import {sqliteTable,text,integer,index} from 'drizzle-orm/sqlite-core';
export const projects=sqliteTable('projects',{id:text('id').primaryKey(),owner:text('owner').notNull(),name:text('name').notNull(),document:text('document').notNull(),revision:integer('revision').notNull().default(1),inviteHash:text('invite_hash').notNull(),updatedAt:integer('updated_at').notNull()},t=>[index('idx_projects_owner').on(t.owner)]);
export const members=sqliteTable('members',{id:text('id').primaryKey(),projectId:text('project_id').notNull(),userId:text('user_id').notNull(),name:text('name').notNull(),lastSeen:integer('last_seen').notNull(),role:text('role').notNull()},t=>[index('idx_members_project_user').on(t.projectId,t.userId),index('idx_members_user').on(t.userId)]);
export const comments=sqliteTable('comments',{id:text('id').primaryKey(),projectId:text('project_id').notNull(),userId:text('user_id').notNull(),name:text('name').notNull(),body:text('body').notNull(),time:integer('time').notNull(),createdAt:integer('created_at').notNull()},t=>[index('idx_comments_project_time').on(t.projectId,t.createdAt)]);
export const mediaFiles=sqliteTable('media_files',{id:text('id').primaryKey(),projectId:text('project_id').notNull(),name:text('name').notNull(),mime:text('mime').notNull(),size:integer('size').notNull(),createdAt:integer('created_at').notNull()},t=>[index('idx_media_project').on(t.projectId)]);
